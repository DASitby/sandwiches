<!-- fixed position navigation bar -->
<nav class="nav-display">
  <div>
    <span class="nav-title" style="cursor: pointer;" onclick="window.location='/listings/';">WE ONLY SELL SANDWICHES DOT COM 🍞</span>
  </div>
  <div>
  <% if (cookie.includes('admin=true')) { %>
      <button class="nav-menu-buttons" onclick="window.location='/create/';">CREATE LISTING</button>
      <button type="submit" class="nav-menu-buttons">MY LISTINGS</button>
      <button type="submit" class="nav-menu-buttons">MY SALES</button>
    <% } else { %>
      <button type="submit" class="nav-menu-buttons">FAVOURITES</button>
      <button type="submit" class="nav-menu-buttons">MESSAGES</button>
    <% } %>
      <button type="submit" class="nav-menu-buttons" onclick="window.location='/search/';">SEARCH</button>
    </div>
    </nav>
